// Generated by WireGen — do not edit.
//
// Compile-time type assertions for keyed @Inject / @Provides
// annotations. The functions below are never called; they
// exist purely so the compiler unifies each key's `BindingKey<T>`
// generic parameter with the consuming binding's type. A
// mismatch surfaces as a Swift compile error attributed via
// #sourceLocation to the user's source line.

import Synchronization
import Wire
import WireTestLibrary
import WireTesting

private func _wireTypeCheck_1() {
    func _check<T>(_: BindingKey<T>, _: T.Type) {}

    #sourceLocation(file: "Tests/IntegrationTests/KeyedExample.swift", line: 18)
    _check(AppName.alternate, AppName.self)
    #sourceLocation()

    #sourceLocation(file: "Tests/IntegrationTests/KeyedExample.swift", line: 22)
    _check(AppName.alternate, AppName.self)
    #sourceLocation()
}

private func _wireTypeCheck_2() {
    func _check<T>(_: BindingKey<T>, _: T.Type) {}

    #sourceLocation(file: "Tests/IntegrationTests/KeyedInitParameterExample.swift", line: 15)
    _check(AppName.boundViaInit, AppName.self)
    #sourceLocation()

    #sourceLocation(file: "Tests/IntegrationTests/KeyedInitParameterExample.swift", line: 21)
    _check(AppName.boundViaInit, AppName.self)
    #sourceLocation()
}
